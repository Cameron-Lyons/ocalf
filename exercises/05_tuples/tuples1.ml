(* TODO: Create the required tuples *)

let pair = ???  (* A pair of int and string: (1, "one") *)
let triple = ??? (* A triple of int, float, bool: (42, 3.14, true) *)

let () =
  assert (pair = (1, "one"));
  assert (triple = (42, 3.14, true));
  print_endline "Tuples created!"
