(* TODO: Use if as an expression to compute the absolute value *)

let abs n =
  ??? (* Use if/then/else as an expression *)

let () =
  assert (abs 5 = 5);
  assert (abs (-5) = 5);
  assert (abs 0 = 0);
  print_endline "If expressions work!"
